# Railway Production Configuration
# Only install production dependencies
production=true
omit=dev

# Disable optional dependencies that require compilation
optional=false

# Use faster install for production
prefer-offline=true
audit=false
fund=false
